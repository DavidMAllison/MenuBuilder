# Lint config — default rules (pyflakes F + pycodestyle errors E4/E7/E9).
# F821 (undefined name) is the class of bug that has crashed the SMS server
# main loop before; keep it fatal.
target-version = "py312"
exclude = [".venv", "test_output", ".playwright-mcp"]
