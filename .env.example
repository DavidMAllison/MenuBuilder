# Secrets — copy to .env and fill in. Monday cutover (Phase 4.5) moves these
# out of config.json; until then config.json remains the live source.
# .env is already gitignored.
YOUTUBE_API_KEY=
ATK_EMAIL=
ATK_PASSWORD=
FLASK_SECRET_KEY=
